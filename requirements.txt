# AI Learning Recommender - Minimal Dependencies
# FastAPI Backend with SQLite (No Docker Required)

# Web Framework
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0
python-multipart==0.0.6

# WebSocket support
websockets==12.0

# Note: Using SQLite (built-in Python), no external database needed
# Note: Using simplified algorithms, no heavy ML libraries needed
# Note: No Redis/Kafka/S3 needed for local development
